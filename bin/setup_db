#!/usr/bin/env ruby

require_relative '../config/database'

puts 'Setting up database...'
result = DatabaseConfig.setup
if result
  puts 'Database setup complete!'
else
  puts 'Database setup skipped (sqlite3 not available - using in-memory storage)'
end

